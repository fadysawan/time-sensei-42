# API Configuration
VITE_API_BASE_URL=http://localhost:8080/api/v1

# OAuth2 Configuration (Backend handles OAuth2)
# OAuth2 endpoints are at /api/oauth2/ (not /api/v1/oauth2/)
# Frontend will automatically adjust URLs for OAuth2 redirects